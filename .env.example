# Port the whisper HTTP server listens on inside the container
PORT=8778

# Whisper model to use (baked into the image at build time)
# Options: tiny.en, base.en, small.en, medium.en
# base.en is the default — good balance of speed and accuracy on CPU
WHISPER_MODEL=base.en
